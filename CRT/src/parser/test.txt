// TEST CONSTRAINTS FILE
DOMAINS:
  A : [1,...,5],
  C : [8...9],
  B : [1,...,3]
;

CONSTRAINTS:
 /* This does not make any sense, it's a test file
   showing all possible constraint operators.
  */
  A < B * C,
  B <= C + A,
  A > C,
  B >= C,
  42 /= 56,
  A = B
;